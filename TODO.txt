
 When ws connection on server disconnects make the server send "peer disconnect" message to all client that have previously asked to be connected to the disconnecting client.





- test refreshing browser to find state where friends lose online status (and see how messages get passed when back online)
- on a friend page highlight latest transaction(s) since the page was last visited

- IOU:n lähetys cachettamalla severiin jos kaveri ei online
  * message quet jotka lähetetään kun niiden vastaanottaja on online
  * message quet myös clientteihin, eli kun kaveri tulee online, varmistetaan onko hän saanut viestin (serveristä tai muualta) - ja jos ei ole lähetetään itse

- Jos sama käyttäjä on auki kahdella eri laitteella (esim. kun kirjautuu priva avaimella), luo webrtc yhteydet niiden välille ja synkkaa kaikki tiedot / toiminnot


CODE
- should there be some UI framework to simplify rendering?


Joskus myöhemmin:
- Currencies sivu
  * listaa kaikki itse käytetyt ja kaverien käyttämät valuutat
  * Sama ticker voi olla eri valuutoilla, mutta ei samaa selostusta valuutasta
  * listaa suosituimmuus järjestyksessä, joten ei haittaa vaikka joku "spämmää"



Scratch book

On add a friend page at the top of the main box show label "My key" followed by users public key in text input that can't be edited. On right side of text input show copy button (just an icon) that copies the key on clipboard. Clicking on the text will select it (so user can also copy from there). Make text field as wide as possible, but all of the key don't need to be visible (more important is having concurring sytles with the rest of the page).

on friends page list keep friends name on one row (use textoverflow: ellipsis). Also make a green version of friend icon, and when there is active webrtc connection between you and that friend, show the green icon instead of the normal gray icon.


Security questions
- when server sends "peer_connect" message for two peers to connect, do the clients still verify that the peer they are connecting to is still their friend? The security should always stay on client, so even conncting to malicious server should not be able to damage client data
  * NOTE: messages between peers should always be encrypted anyways






Create a migration process (checks app version and runs migrations when app version change matches) to change IOU -> record, anything else that needs changing?
  * make search for IOU

FEATURES
Add inactivate friend button.
- defaulting / debt forgiveness
- explain possible concequences
Inactive frient
- inactive but shows history
- can be restored if both agree (suggest friendship - but also returns the old state)
- can be fully removed (same button)
  * no notification to other?
  * as long as either retains inactive state, reactivation can be suggested




BUGS:
- with multiple devices, only one of them shows friends online status correctly - webrtc connection to only one of them?
  * add e2e test case for multiple devices online
    * test to see online status (current bug)
    * fanning messages to devices
- getting name passed to second device still bugs.. (brave user with incognito firefox)

CHECK
- test deleting user and make sure indexdb is fully removed

SMALL
- on a friend page highlight latest transaction(s) since the page was last visited




MAJOR

- Multiple relay servers to connect to
  * fanning all messages through all connected servers
  * settings page allows adding / removing new servers
  * needed before https://iou-ui.up.railway.app/ -> app.tally.earth move can be made (to move users)

- Currencies sivu
  * listaa kaikki itse käytetyt ja kaverien käyttämät valuutat
  * Sama ticker voi olla eri valuutoilla, mutta ei samaa selostusta valuutasta
  * listaa suosituimmuus järjestyksessä, joten ei haittaa vaikka joku "spämmää"


- Tracking of unique favours (NFT's)
  * e.g. Bob gave me his powerdrill - no need to define value in euros. It's either to remind to give that exact powerdrill back later, or something of same value.


Scratch book:

